// PaginationCounter.js
import React from "react";

const PaginationCounter = ({ currentPage }) => {
  return <div className="pagination-counter"> {currentPage}</div>;
};

export default PaginationCounter;
